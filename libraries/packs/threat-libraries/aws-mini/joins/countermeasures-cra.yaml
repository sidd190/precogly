framework: cra-2024

mappings:
  # ============================================================================
  # Annex I.1(b) — No known exploitable vulnerabilities
  # ============================================================================
  - countermeasure: lambda-code-signing
    requirements:
      - "Annex I.1(b)"
    sufficiency: partial

  - countermeasure: apigw-waf
    requirements:
      - "Annex I.1(b)"
    sufficiency: partial

  # ============================================================================
  # Annex I.1(c) — Secure by default configuration
  # ============================================================================
  - countermeasure: s3-block-public-access
    requirements:
      - "Annex I.1(c)"
    sufficiency: full

  - countermeasure: lambda-least-privilege
    requirements:
      - "Annex I.1(c)"
    sufficiency: full

  - countermeasure: apigw-request-validation
    requirements:
      - "Annex I.1(c)"
    sufficiency: partial

  # ============================================================================
  # Annex I.1(d) — Protection against unauthorized access
  # ============================================================================
  - countermeasure: apigw-cognito-auth
    requirements:
      - "Annex I.1(d)"
    sufficiency: full

  - countermeasure: apigw-api-key
    requirements:
      - "Annex I.1(d)"
    sufficiency: partial

  - countermeasure: s3-bucket-policy
    requirements:
      - "Annex I.1(d)"
    sufficiency: full

  - countermeasure: dynamodb-fine-grained-access
    requirements:
      - "Annex I.1(d)"
    sufficiency: full

  - countermeasure: sqs-queue-policy
    requirements:
      - "Annex I.1(d)"
    sufficiency: full

  - countermeasure: mutual-tls
    requirements:
      - "Annex I.1(d)"
    sufficiency: full

  # ============================================================================
  # Annex I.1(e) — Confidentiality of data (encryption)
  # ============================================================================
  - countermeasure: s3-encryption
    requirements:
      - "Annex I.1(e)"
    sufficiency: full

  - countermeasure: dynamodb-encryption
    requirements:
      - "Annex I.1(e)"
    sufficiency: full

  - countermeasure: sqs-encryption
    requirements:
      - "Annex I.1(e)"
    sufficiency: full

  - countermeasure: tls-encryption
    requirements:
      - "Annex I.1(e)"
    sufficiency: full

  - countermeasure: vpc-private-link
    requirements:
      - "Annex I.1(e)"
    sufficiency: partial

  - countermeasure: lambda-secrets-manager
    requirements:
      - "Annex I.1(e)"
    sufficiency: full

  # ============================================================================
  # Annex I.1(f) — Integrity of data, commands, and programs
  # ============================================================================
  - countermeasure: request-signing
    requirements:
      - "Annex I.1(f)"
    sufficiency: full

  - countermeasure: s3-mfa-delete
    requirements:
      - "Annex I.1(f)"
    sufficiency: full

  - countermeasure: s3-versioning
    requirements:
      - "Annex I.1(f)"
    sufficiency: partial

  - countermeasure: lambda-code-signing
    requirements:
      - "Annex I.1(f)"
    sufficiency: full

  - countermeasure: lambda-input-validation
    requirements:
      - "Annex I.1(f)"
    sufficiency: full

  - countermeasure: apigw-request-validation
    requirements:
      - "Annex I.1(f)"
    sufficiency: partial

  - countermeasure: sqs-message-validation
    requirements:
      - "Annex I.1(f)"
    sufficiency: full

  # ============================================================================
  # Annex I.1(g) — Data minimization
  # ============================================================================
  - countermeasure: dynamodb-fine-grained-access
    requirements:
      - "Annex I.1(g)"
    sufficiency: partial

  - countermeasure: lambda-least-privilege
    requirements:
      - "Annex I.1(g)"
    sufficiency: partial

  - countermeasure: s3-bucket-policy
    requirements:
      - "Annex I.1(g)"
    sufficiency: partial

  # ============================================================================
  # Annex I.1(h) — Availability and resilience
  # ============================================================================
  - countermeasure: lambda-reserved-concurrency
    requirements:
      - "Annex I.1(h)"
    sufficiency: partial

  - countermeasure: apigw-throttling
    requirements:
      - "Annex I.1(h)"
    sufficiency: full

  - countermeasure: dynamodb-pitr
    requirements:
      - "Annex I.1(h)"
    sufficiency: full

  - countermeasure: sqs-dlq
    requirements:
      - "Annex I.1(h)"
    sufficiency: partial

  # ============================================================================
  # Annex I.1(i) — Minimize negative impact on other services
  # ============================================================================
  - countermeasure: lambda-vpc
    requirements:
      - "Annex I.1(i)"
    sufficiency: full

  - countermeasure: dynamodb-vpc-endpoint
    requirements:
      - "Annex I.1(i)"
    sufficiency: full

  - countermeasure: vpc-private-link
    requirements:
      - "Annex I.1(i)"
    sufficiency: full

  - countermeasure: api-gateway-private
    requirements:
      - "Annex I.1(i)"
    sufficiency: full

  # ============================================================================
  # Annex I.1(j) — Reduce attack surfaces
  # ============================================================================
  - countermeasure: apigw-waf
    requirements:
      - "Annex I.1(j)"
    sufficiency: partial

  - countermeasure: api-gateway-private
    requirements:
      - "Annex I.1(j)"
    sufficiency: full

  - countermeasure: dynamodb-vpc-endpoint
    requirements:
      - "Annex I.1(j)"
    sufficiency: partial

  - countermeasure: lambda-vpc
    requirements:
      - "Annex I.1(j)"
    sufficiency: partial

  - countermeasure: s3-block-public-access
    requirements:
      - "Annex I.1(j)"
    sufficiency: full

  # ============================================================================
  # Annex I.1(k) — Limit impact of exploitable vulnerabilities
  # ============================================================================
  - countermeasure: lambda-least-privilege
    requirements:
      - "Annex I.1(k)"
    sufficiency: full

  - countermeasure: lambda-vpc
    requirements:
      - "Annex I.1(k)"
    sufficiency: partial

  - countermeasure: s3-versioning
    requirements:
      - "Annex I.1(k)"
    sufficiency: partial

  - countermeasure: dynamodb-pitr
    requirements:
      - "Annex I.1(k)"
    sufficiency: partial

  # ============================================================================
  # Annex I.1(l) — Recording and monitoring internal activity
  # ============================================================================
  - countermeasure: apigw-cloudwatch-logging
    requirements:
      - "Annex I.1(l)"
    sufficiency: full

  - countermeasure: s3-access-logging
    requirements:
      - "Annex I.1(l)"
    sufficiency: full

  # ============================================================================
  # Annex I.2(f) — Secure update distribution mechanism
  # ============================================================================
  - countermeasure: lambda-code-signing
    requirements:
      - "Annex I.2(f)"
    sufficiency: full
