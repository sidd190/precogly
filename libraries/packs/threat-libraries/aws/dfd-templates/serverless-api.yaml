template:
  slug: aws-serverless-api
  name: "AWS Serverless API"
  description: "Serverless REST API with Lambda, API Gateway, and DynamoDB"
  category: api
  diagram_type: level1

canvas_data:
  nodes:
    # External actor (outside all boundaries)
    - id: "actor-client"
      type: "humanActor"
      position: { x: 50, y: 200 }
      data:
        label: "Client"
        actorType: "external"
        description: "Web or mobile application"

    # System boundary - AWS Serverless System
    - id: "sb-aws"
      type: "systemScope"
      position: { x: 180, y: 50 }
      style: { width: 720, height: 450 }
      data:
        label: "AWS Serverless API"
        owner: "API Team"
        classification: "Production"

    # Trust boundary - AWS VPC (inside sb-aws)
    - id: "tb-aws"
      type: "trustZone"
      parentId: "sb-aws"
      position: { x: 30, y: 60 }
      style: { width: 650, height: 360 }
      data:
        label: "AWS VPC"
        zoneType: "privateSecured"
        technology: "Amazon VPC"

    # AWS Services (nested inside tb-aws)
    - id: "process-apigw"
      type: "process"
      parentId: "tb-aws"
      position: { x: 50, y: 80 }
      data:
        label: "API Gateway"
        component_ref: "api-gateway"
        description: "REST API endpoint with throttling and auth"

    - id: "process-cognito"
      type: "process"
      parentId: "tb-aws"
      position: { x: 50, y: 200 }
      data:
        label: "Cognito"
        component_ref: "cognito"
        description: "User authentication and authorization"

    - id: "process-lambda"
      type: "process"
      parentId: "tb-aws"
      position: { x: 250, y: 80 }
      data:
        label: "Lambda Function"
        component_ref: "lambda"
        description: "Serverless compute for business logic"

    - id: "datastore-dynamo"
      type: "datastore"
      parentId: "tb-aws"
      position: { x: 450, y: 80 }
      data:
        label: "DynamoDB"
        component_ref: "dynamodb"
        description: "NoSQL database for application data"
        dataSensitivity: "confidential"

    - id: "datastore-s3"
      type: "datastore"
      parentId: "tb-aws"
      position: { x: 450, y: 200 }
      data:
        label: "S3 Bucket"
        component_ref: "s3"
        description: "Object storage for files and assets"

  edges:
    - id: "edge-1"
      source: "actor-client"
      target: "process-apigw"
      type: "dataFlow"
      data:
        label: "API Requests"
        protocol: "HTTPS"
        encrypted: true

    - id: "edge-2"
      source: "process-apigw"
      target: "process-cognito"
      type: "dataFlow"
      data:
        label: "Auth Check"
        protocol: "HTTPS"
        encrypted: true

    - id: "edge-3"
      source: "process-apigw"
      target: "process-lambda"
      type: "dataFlow"
      data:
        label: "Invoke Function"
        protocol: "AWS Internal"
        encrypted: true

    - id: "edge-4"
      source: "process-lambda"
      target: "datastore-dynamo"
      type: "dataFlow"
      data:
        label: "Read/Write Data"
        protocol: "HTTPS"
        encrypted: true

    - id: "edge-5"
      source: "process-lambda"
      target: "datastore-s3"
      type: "dataFlow"
      data:
        label: "Store Files"
        protocol: "HTTPS"
        encrypted: true
